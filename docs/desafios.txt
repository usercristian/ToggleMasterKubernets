codigos fonte das aplicações estavam incorretas
dependencias declaradas incorretamente
eks do academy possui limitações de add-on
foi necessario utilizar o terraform para economia nos builds
arquivo db nativo do docker-compose não funciona no RDS da aws
criado job somente para popular os bd do RDS para os 3 serviços
foi necessario utilizar a vpc padrao pois a role do academy nao permite nat-gateway
sg adaptado para testes e eks
equilibrado entre modularidade e planificação dos diretorios

criar o eks no console devido limitações do lab ficar atento aos add-ons

nano ~/.aws/credentials
nano ~/.kube/config
aws eks update-kubeconfig --region us-east-1 --name togglemaster-ks
kubectl get namespace
kubectl get svc -n togglemaster ingress-nginx-controller

antes de dar o apply, validar o secret.yaml,.venv e roda o gerar_secrets.sh
coletar o ecr e fazer upload das imagens do docker usando o script_ecr
fazer o kubectl apply -k .
ele que vai carregar os manifestos para dentro dos pods